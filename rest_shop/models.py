from django.contrib import admin
from shop.models import Dish, Category, Company, Cart, CartContent
admin.register(Dish)
admin.register(Category)
admin.register(Company)
admin.register(Cart)
admin.register(CartContent)
# Create your models here.
